# 本地 npm run dev / make start 时，/v3/cube-api 代理到的后端（默认线上）
# CUBE_API_UPSTREAM=https://cubing.pro/v3/cube-api

# 联调本机 Go 后端（0.0.0.0:20000）：make dev
# NEXT_PUBLIC_DEV_API_BASE=http://127.0.0.1:20000/v3/cube-api
