/**
 * 路由表入口 — 与研发手册 `config/routes.ts` 路径对齐
 * 实现位于 `src/config/routes.ts`
 */
export {
  APP_ROUTES,
  matchRoute,
  pathnameToHref,
  type AppRoute,
  type ImplType,
  type RenderMode,
} from "../src/config/routes";
